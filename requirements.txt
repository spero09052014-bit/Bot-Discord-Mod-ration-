# ============================================
# DÉPENDANCES — Bot Kōtei 🍥
# ============================================
# Installation : pip install -r requirements.txt

discord.py>=2.4.0
python-dotenv>=1.0.1

# Mini serveur web utilisé par keep_alive.py pour rester éveillé
# sur l'offre gratuite "Web Service" de Render (ping UptimeRobot).
flask>=3.0.0

# Utilisé uniquement par la commande !resume (résumé intelligent via Claude).
# Le bot fonctionne sans, mais !resume basculera alors en mode "simplifié".
anthropic>=0.34.0
